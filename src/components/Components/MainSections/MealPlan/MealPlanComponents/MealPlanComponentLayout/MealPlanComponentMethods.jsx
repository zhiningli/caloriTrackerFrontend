export const expandAction = (agParams, evtSrc) =>{
    alert('Expand a dropdown to display a table of food items in this meal');
};

export const expandBtnSetProps = (actionName, btnProps, agParams) => {
    let props = {...btnProps};

    return props;
};


export const editAction = (agParams, evtSrc) =>{
    alert('Expand a dropdown to display a table of food items in this meal');
};

export const editBtnSetProps = (actionName, btnProps, agParams) => {
    let props = {...btnProps};

    return props;
};


export const deleteAction = (agParams, evtSrc) =>{
    alert('Expand a dropdown to display a table of food items in this meal');
};

export const deleteBtnSetProps = (actionName, btnProps, agParams) => {
    let props = {...btnProps};

    return props;
};